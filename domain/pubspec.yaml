name: domain
version: 0.0.1
publish_to: none
description: Project domain module

environment:
  sdk: ">=3.0.0 <3.3.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  data:
    path: ../data
  core:
    path: ../core
  core_ui:
    path: ../core_ui
  navigation:
    path: ../navigation
  auth:
    path: ../feature/auth
  settings:
    path: ../feature/settings


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.3.3

flutter:
